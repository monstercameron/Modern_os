import { AppShell } from '../components/AppShell';

export function NotesApp() {
  return (
    <AppShell
      icon="📝"
      title="Notes"
      description="Capture your thoughts and ideas with a simple note-taking interface."
    />
  );
}
